# 기본원리 (주석)

1. 객체생성 <- = -> 
  
a <- 5
b = 5
10 -> c
a+b+c
boxplot(mtcars$wt) -> b1
b1$out

2. 함수 ()

a <- c(2,5,8,9,3,4,5,5,1,3,5,7)
mean(a)   # 평균
median(a)  # 중앙값, 중위수
min(a)     # 최솟값
max(a)     # 최댓값
sum(a)    # 총합
range(a)  # 범위
IQR(a)    # 사분위범위
var(a)    # 표본분산
sd(a)     #표본표준편차

3. package
map('world')
install.packages("maps")
library(maps)
map('world')
